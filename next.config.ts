import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 外部画像ドメインは現在未使用のため images 設定なし。
  // 将来 next/image で外部URLを使う場合は images.remotePatterns を追加すること。
};

export default nextConfig;
